# 2026-06-14 · 前端整体重做「干净亮色 SaaS」+ 创业级功能 + 渐进式 SSE

> 承接 2026-05-26「暗色终端」版；老板拍板改方向为**干净亮色 SaaS**（Linear/Vercel 血统），
> 并补齐"像真产品/可创业"的闭环功能与一批稳定性修复。本文记录这一轮全部改动与验收。

## 1. 方向变更
- 从「精炼暗色·情报终端 #0a0f1a」改为 **干净亮色 SaaS**：白底 + 大留白 + 1px 发丝线，
  **车速红 `#dc2626` 单点强调**，Geist + Geist Mono「等宽元数据」标签做签名，
  把双脑 Agent（意图徽章 / 思考链 / 可展开 SQL）做成视觉主角。
- 流程：先用对话样机给老板看三个方向 → 选亮色 SaaS + 深度改造 → 自主实现。

## 2. 设计系统重建（含修真 bug）
- `tokens.css` 重写为**完整超集**：新语义令牌（`--ink/--accent/--line/--bg-*/--r-*/--sh-*`）
  + 历史命名 alias 全部指过去，旧组件零改动跟随新配色。
- **修复**：多组件曾引用 `tokens.css` 里**根本没定义**的变量（`--bg-1-2/--data-dim/--up/
  --up-bg/--border-soft/--brand-border/--err/--maxw-thread` 等）→ 工作树原本半残；现一次补齐。
- `global.css` 补齐组件依赖却缺失的 keyframes（`spin/skeletonPulse` 等）。
- 校验：`grep var(--…)` 全量 token 与 tokens.css 定义做差集 = **空**（无未定义变量）。

## 3. 重写/新增组件
- 重写：`App`（命令中心↔对话双态 + 移动端抽屉）、`Sidebar`（会话按时间分组/搜索/收藏看板入口/
  用户菜单）、`TopBar`、`ThinkingTrace`（横向步进器 + 意图徽章）、`UserMessage`、
  `AssistantMessage`（统一操作栏）、`DataResultCard`（+**查看 SQL 折叠**展示 Text2SQL）、
  `KnowledgeAnswer`、`Composer`、`QuickSuggest`、`LoginView`、`KnowledgeBaseModal`、`LogoMark`（准星镜头）。
- 新增：`CommandCenter`（门面 Hero + 居中命令输入 + 按双脑能力分组示例 + 引擎概览）、
  `BoardModal`、`PublicShare`、`Toasts`。
- `utils/chart.js`：ECharts 暗色主题改为**亮色主题**（发丝网格 / 等宽数字轴 / 车速红系列色）。
- 删除 orphan：`MarketKPI / MarketOverview / EmptyState`（确认无引用）。
- `index.html`：`html` 底色暗→亮 `#fafafa`（防暗闪）、favicon 改红镜头、Geist 补 800 字重。

## 4. 创业级新功能（前端 + 最小后端）
- **收藏看板**：结果卡「收藏」存快照 → `BoardModal` 网格回看/重提/删除。
  `useBoard` + `api/board.js`（mock=localStorage / live=后端）；后端 `SavedInsight` 表 +
  `GET/POST/DELETE /api/insights`（JWT + user_id 隔离）。
- **一键分享**：生成只读公开链接 `/s/{token}`，`PublicShare.vue` 带品牌渲染（复用结果卡）=营销位。
  `api/share.js`；后端 `SharedInsight` 表 + `POST /api/share` + **免鉴权** `GET /api/public/share/{token}`；
  `main.js` 按 `location.pathname` 分流（不引 vue-router；生产靠 Caddy `try_files` 回 index.html）。
- **导出报告**：`utils/report.js` 一键导出 Markdown（结论+表+引用+SQL+品牌页脚）；全局 `Toast` 反馈。

## 5. 后端稳定性 / 体验修复（核对了"另一 agent 的 bug 清单"）
- **已是修好状态（清单基于旧版本）**：0 行不调 LLM（insight 节点 A/B/C 三路）、0 行不出图、
  compose 不误标【数据分析】、few-shot 已无"奔驰"、DOMAIN 有完整品牌白名单、charts 维度按**类型**
  而非列位选、CSS 变量已全定义 —— 当前代码均已正确，无需改。
- **真正新修**：
  - **渐进式 SSE（O4）**：`graph.stream_agent` 用 `_GRAPH.stream(values)` 逐超步产出快照；
    `main.py /api/ask` 改为**工作线程跑图 + `asyncio.Queue` 桥回事件循环**，字段一就绪就推对应事件
    （intent/sql/rows/chart/insight/citation 各推一次）——告别"整图算完一次性全出"。
  - **多轮上下文（O3）**：`_load_history` 取本会话最近几轮喂进意图路由与 gen_sql（`_history_block`），
    "那丰田呢"可解析指代。
  - **LLM 配置自检（O7）**：启动时 base_url 与 model 跨厂商不一致 → 告警（防 qwen 默认值 + deepseek base 静默连错）。
  - **mock 覆盖外品牌（O11/B8）**：问"奔驰销量"等 → mock 模拟真实后端"查不到不编造"（出 SQL、0 行、老实告知）。
- `30-app-schema.sql` 补 `saved_insight / shared_insight` 表（dev SQLite 走 create_all 自动建）。

## 6. 验收
- `npm run build`：618 模块编译通过。
- 后端 `TestClient` E2E：收藏增/查/删、分享→公开读取、坏 token 404、未鉴权 401（隔离）全过；
  **渐进式 SSE** 用假图（免 LLM）验证事件序 `stage→intent→sql→rows→chart→insight→done` + 落库正确。
- Playwright（mock）浏览器冒烟：登录 / 命令中心 / 数据卡（红渐变柱 + SQL 折叠 + 操作栏）/
  知识卡（意图徽章 + 引用溯源）/ 收藏看板 / **0 行不出图只给提示**，全程**零控制台报错**。

## 7. 待办 / 边界（诚实）
- `insight` 单条文本仍是整段生成后按 24 字切片回放，**非 LLM token 级**（`app/insights.py` 有 `chat_stream` 雏形，留作演进）。
- 本机无 `docx` 全局库与 `mermaid-cli`：本轮 `.md`（面试手册 §6.1/§6.2.1）已更新，**对应 `.docx` 待在有工具链的机器上 `_md_to_word.js` 重渲**；PRD-2 §9.1/§10 若要同步节点级流式 + 5 个新端点，需改 `build_prd2.js` 后重生成。
- O2（意图纯关键词）、O6（insights.py 与 graph.insight 重复）保留为演进项，未动以免扰动已记录的评测口径（意图 89% / Text2SQL 78.3%）。
