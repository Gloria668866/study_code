# 04 · 交付物与进度

## 已产出文件
### 文档（docs/prd/）
- `PRD-1-数据获取与处理.docx` —— 含 §7.1「schema 是怎么定出来的」方法论；已去合规红线改为「采集策略(防封)」；4 张流程图。
- `PRD-2-Agent构建与整体项目.docx` —— v2 深化版，20 章 6 图。含 Text2SQL 输入/输出规约+异常对策+重试降级、选图机制(非多模态澄清)、LangGraph 节点设计、应用层存储字段、安全与权限(§17)、NFR(§18)、**§19 分方向实施细则(角色可执行)**、§20 简历包装。
- `开发任务拆解书.docx` —— 14 个工单(T1–T14)，每个含可直接发的提示词。
- `面试深度理解手册.docx`(+.md, 2026-05-26) —— **秋招面试备战核心**。8 模块(定位/数据/Text2SQL/RAG/LangGraph/工程/评测/部署)+ 跨模块系统设计追问 + 实测数字附录，11 张 mermaid 图，每点按「做了什么→为什么→代码在哪→大厂怎么追问→标准答+陷阱/诚实边界」。`.md` 改完用 `_md_to_word.js` 重渲 Word。已标 ⚠️必须对齐口径(MinerU 是 lite/Schema Linking 库小走全量/graph.py 是真路径)。
  - **实测评测数字(eval/reports/，背下来)**：Text2SQL 执行准确率 **78.3%**(60题,带重试,首次76.7%,重试增益+1.7pp——因重试只治"执行报错"不治"能跑但语义错")；RAG **ctxP0.92/ctxR0.89/faith0.80/relv0.94**(60题)、防幻觉拒答 **100%**(12/12)、冲突处理 **100%**(6/6)、消融 vec0.75/hybrid0.75/+rerank0.762(demo库小增益不明显)；意图路由 **89%**(100题,错分主要 rag→sql)；数据质量 18/18。
  - **sqlglot 升级(2026-05-26)**：`app/sql_guard.py` 由 sqlparse 改为 **sqlglot 解析 AST**(单条SELECT/find_all遍历子树拦写操作/fail-closed)，与 README/PRD/PROJECT-MEMORY 的"sqlglot"口径终于一致；`requirements.txt` sqlparse→sqlglot；10单测+对抗用例全过、端到端复测正常。
  - **仓库整理(2026-05-26)**：删根目录废弃 starter 部署文件(docker-compose.yml/Dockerfile/Caddyfile，被 deploy/ 取代)；团队协作规范.md 移进 docs/；README 重写成车市镜产品门面(双脑/架构图/目录导航/快速开始)。

### 文档生成方式（可重生成）
- 生成脚本：`docs/prd/build_prd1.js` / `build_prd2.js` / `build_tasks.js`（Node + docx 库）。
- 重生成命令：`NODE_PATH=C:/Users/Lenovo/AppData/Roaming/npm/node_modules node build_xxx.js`
- 流程图源：`docs/prd/diagrams/*.mmd`；渲染：`mmdc -i x.mmd -o x.png -c mmdc.json -p puppeteer.json -b white -s 2`（mermaid-cli 全局已装）。
- 注意：docx 被 Word 打开时会锁文件，脚本会自动写到 `-已更新.docx`；Word 打开后目录需右键「更新域」刷页码。

### 代码/数据
- `sql/schema.sql` —— 已定死的分析库表结构（见 03）。
- `data/probe/` —— T1 探针脚本 + 真实样本 + 字段清单。
- `data/clean_load.py` —— 清洗入库脚本（raw JSONL→清洗→拆6表→SQLite，幂等 UPSERT），见 03/PRD-1 §6。
- 现有 starter 骨架：`app/`（config/db/llm/sql_guard/text2sql/charts/insights/agent/main 等），`seed.py`（旧 demo 数据，已被 clean_load.py 的 DROP 清出库），`bi_demo.db`（已由 clean_load.py 重建为车市镜分析库）。

## 工单全景（依赖排序）
T1 数据源探针(✅) → T2 schema定稿建库 → T3 正式采集(✅部分) → T4 清洗治理(✅) → T5 MinerU知识库 → T6 数据质量 →
T7 Text2SQL(✅本地跑通) → T8 图表洞察 → T9 RAG → T10 LangGraph编排 → T11 FastAPI接口 → T12 前端(✅产品级门面，mock 全通) → T13 测试(✅评测体系+CI) → T14 部署。
关键路径：T1→T2→T3→T4→T7→T10→T11→(T14上线/T13测试)。

## 当前进度（2026-05-22）
- ✅ 产品方向、技术栈、三份 PRD/工单文档
- ✅ T1：真实爬到数据、数据量够、schema 定死、写进 PRD-1
- ✅ T3 采集（部分）：**正式全量采集已跑通并落原始层（未入库）**
  - 脚本 `data/crawl_sales.py`；产物 `data/raw/sales_rank_raw.jsonl`（**7,734 行**）+ `data/raw/_crawl_summary.json`
  - 覆盖 **409 车系 × 28 个月（2024-01~2026-04）× 纯电/插混/增程**（纯电4923/插混2528/增程283）
  - 数据质量实测：count 异常 0 条；score 全 0（→清洗置 NULL）；last_rank 缺失 337 行（→标「新上榜」）
  - **按用户要求：先不入库，等清洗。** 采集方法已详写入 PRD-1 §5.5；清洗逻辑（字段级规则+去重+实体对齐+口径+raw→DWD/DWS 映射）已详写入 PRD-1 §6.1–6.6
- ✅ T4 清洗入库 + T7 Text2SQL（2026-05-22，**后端整链路本地跑通**）
  - 脚本 `data/clean_load.py`：按 PRD-1 §6 清洗 `data/raw/sales_rank_merged.jsonl`（8072 行）→ 拆 6 表 → 入 SQLite `bi_demo.db`（零配置，PostgreSQL 留生产）
  - 实测行数：`fact_sales_rank=8072`、`fact_price=8072`、`fact_review=8072`、`dim_series=409`、`dim_brand=101`、`dim_date=29`；新上榜(last_rank→NULL)=355 行；volume 异常 0 条；powertrain 纯电5122/插混2646/增程304
  - 清洗要点（与 §6 一致）：`_month`→date_id/year/month/quarter/ym；`count`→volume；`score`一律 NULL；`last_rank` 0/缺失→NULL 且标新上榜；powertrain 由 `_new_energy_type` 推断；`price/dealer_price` 正则解析「x-y万」、暂无报价→NULL；事实 `(series_id,date_id,new_energy_type,rank_type)` UPSERT 幂等；并 DROP 掉 seed.py 留下的 starter 旧表
  - `app/text2sql.py`：删旧「区域/经销商」few-shot，换成车市镜星型模型的领域提示(DOMAIN)+3 条 few-shot
  - 验收通过：deepseek-v4-pro 对「理想和小米SU7谁卖得多」等真实问题**第 1 次就生成正确 SQL** 并返回真实销量（小米SU7 460,536 > 理想L6 387,948）。注：该 key 的 /models 现仅提供 `deepseek-v4-flash`/`deepseek-v4-pro`（`deepseek-chat` 已不在列表），`.env` 用 v4-pro
  - 详细工作记录：`docs/工作记录/后端/2026-05-22-T4+T7-跑通后端.md`（逐字段逻辑+为什么+2 张 mermaid 图）
- ✅ T12 前端门面（2026-05-22，**产品级，mock 链路全通；后端 live 已预留+做协议兼容**）
  - 新建 `frontend/`：Vue 3 + Vite + ECharts + 原生 POST-SSE（fetch+ReadableStream，因 `/api/ask` 是 POST 而原生 EventSource 只支持 GET）
  - 体现产品逻辑：空状态产品定位+示例问题引导；**双脑分开渲染**（数据分析=图表卡+结论归因+可折叠 SQL/表；知识问答=答案+来源引用卡可溯源）；流式展示 Agent 思考过程（意图→查数据/查文档→出结果）；左栏会话历史+知识库入口；专业克制的数据产品视觉
  - **关键：前端做了 SSE 协议归一化层**——当前后端 `app/main.py` 与 PRD §9.1 有出入（`meta`vs`intent`、`cols`vs`columns`、`sql`/`insight` 推裸字符串、`done`=`[DONE]`、`chart` 推选图建议 `{chart_type,x,y}` 而非完整 ECharts option）。前端两种形态都吃，组件层只认规范事件。**建议后端后续对齐 §9.1（尤其 chart 直接产出完整 option）**
  - **视觉设计系统「车市镜·情报台」**（按真实产品标准重做，非通用聊天框）：品牌标志=镜头光圈+内嵌销量柱线（`LogoMark.vue`，贯穿全局带扫描动效）；配色=暖纸画布+深墨蓝权威+翡翠绿新能源能量色（双脑语义色：数据脑墨蓝/知识脑翡翠）；字体=Fraunces(品牌/大数字)+Manrope(UI)+IBM Plex Mono(代码)，Google Fonts CDN 加载+系统回退；图表翡翠→墨蓝渐变柱+`万`量级格式化；空状态有 hero+数据覆盖背书(409车系/29月/8072记录)。换肤只改 `tokens.css`
  - mock↔live 一个 `.env` 开关（`VITE_DATA_SOURCE`）切换，不动 UI 代码；API 基址走 `VITE_API_BASE`，为上线预留
  - 验收：`npm run dev` 起得来；Playwright 冒烟 mock 链路（图表/思考过程/意图标签/SQL 折叠/RAG 3 引用卡/知识库弹窗）全过、无控制台报错；`npm run build` 编译通过
  - CORS：`app/main.py` 已 `allow_origins=["*"]` 放行 5173，OK（上线收紧时保留前端域名）
  - 详细工作记录：`docs/工作记录/前端/2026-05-22-前端门面.md`（含「SSE 事件→双脑分流渲染」mermaid 图、运行步骤、协议差异表）
- ✅ T12 前端续作：知识库 + 历史会话 + 撤 SSE 兼容层（2026-05-25，承接后端⑤对齐 §9.1）
  - **撤掉 SSE 归一化兼容层**：后端已对齐 §9.1，`events.js` 不再「两种形态都吃」，直接按标准事件名/JSON 解码（`intent{intent,confidence,conversation_id}`/`sql{sql_text}`忽略/`rows{columns,rows}`/`chart`描述符/`insight{delta}`/`citation`**逐条**/`done{msg_id,...}`/`error`）。⚠️ 关键变更：后端 **citation 一条一个事件**逐条推 → 前端解码成单条、`useChat` 累加（原先按数组替换会丢引用）
  - **知识库**（`KnowledgeBaseModal.vue` + 新 `api/kb.js`）：选文件 → `POST /api/kb/upload` → 轮询 `GET /api/kb/{id}` 状态 `parsing→ready/failed` 显进度 → 列表(`/api/kb/list`)/删除(`DELETE /api/kb/{id}`)；前端类型(pdf/html/md/txt)+大小(≤20MB)校验；全部带 Bearer，按 user_id 隔离
  - **引用溯源**（`KnowledgeAnswer.vue`）：引用卡展示 `heading_path`(章节路径，拆成层级 chip)+页码，点击就地展开「出处定位」（《标题》·章节·页）；字段对齐后端 §5.5 `{doc_id,page_no,chunk_id,heading_path,title}`
  - **历史会话还原**（新 `api/history.js` + `useChat.js`）：live 模式 `GET /api/history` 列会话、点开 `GET /api/history/{id}` 还原**含图表/引用**的对话（后端 `result_meta` 存 chart/列行/citations，前端按需懒加载重绘）；mock 模式仍用 localStorage 富副本
  - 数据分析卡确认仍正常：只「图表+一句话结论」、无 SQL/数据表、图型切换器(柱/折/饼/横条)带图例、前端重绘（沿用 §5.3）
  - 验收：Playwright 冒烟 mock 链路全过——登录守卫+演示登录 / SQL(canvas+4切换器+无SQL无表+重绘) / RAG(3引用+heading_path+溯源框) / 历史还原图表 / 知识库(上传→解析中→就绪→删除) / **零控制台报错**；`npm run build` 通过
  - 详细：`docs/工作记录/前端/2026-05-25-知识库与历史会话.md`（含撤兼容层后的「SSE §9.1→双脑渲染」图、KB 上传轮询时序、引用结构）。⚠️ live 联调上传时后端须以 `APP_DATABASE_URL=PG` 启动（kb_document.user_id 外键指 PG users）
- ✅ 前端定调「精炼暗色·情报终端」+ 字体换 Geist（2026-05-26，UI 改版，老板拍板暗色方向）
  - 从「暖纸 #f3f1ea + Fraunces 衬线（杂志气质）」整体改为「冷调暗色 #0a0f1a + Geist 无衬线 + 单一翡翠强调 + azure 数据脑色」（参考 Linear/Vercel/Perplexity/Raycast 的 pro 工具质感）
  - **token 驱动**：只改 `tokens.css` 的值、变量名不动 → 组件自动暗色化；**换肤铁律保住**（后续调色只动 tokens.css）
  - **ink 一词两用陷阱**：`--ink-*` 重定义为「冷光文字阶」（亮→暗，标题/描边自动变亮）；原「ink 当深色背景」逐个改深色面(panel/side-bg)/强调(jade/azure)：登录左面板、助手头像、用户气泡、stop 键、TopBar 头像、数据卡切换器/顶条/圆点、空状态卡条
  - 字体 Geist + Geist Mono（index.html 换 Google Fonts）；ECharts 暗色主题（冷光调色板/轴 #7c8ba3/网格白低透/柱渐变翡翠→azure/tooltip 加深）；global.css body 暗背景 + azure&翡翠辉光 + 极淡网格
  - **踩坑**：①TopBar `.topbar` 硬编码暖白 `rgba(255,253,249,.82)`（非 token）→暗色化后导航条漏白（用户当场发现），改 `rgba(13,20,34,.72)` 毛玻璃；②`--ink-950` 改为最亮文字后，`gradient(...,ink-950)` 当背景处（登录面板/头像/stop:hover）全漏白→逐个改；③hero 标题 jade-700/tx-500 对比度不足→提亮 jade-300/tx-700（WCAG AA）；④Playwright 对 vite dev 报 ERR_EMPTY_RESPONSE→改 build 产物 + http.server 静态托管 + `--no-proxy-server`
  - 验收：`npm run build` 通过；Playwright mock 冒烟全流程零报错；**live 联调通过**（demo 登录真后端→提 SQL→真 SSE 出图表+真实结论，零报错）；截图自查登录/空状态/数据卡
  - 详细 + 前后对比截图：`docs/工作记录/前端/2026-05-26-暗色终端改造.md`（含 img/dark-*.png）
- ✅ 前端整体重做「干净亮色 SaaS · 车速红」+ 创业级功能（2026-06-14，**老板拍板重做，build+后端E2E+浏览器冒烟全过**）
  - **方向变更**：从「精炼暗色·情报终端」改为 **干净亮色 SaaS**（Linear/Vercel/Notion 血统），车速红 `#dc2626` 单点强调；签名 = Geist + Geist Mono「等宽元数据」标签 + 1px 发丝线 + 大留白 + 把双脑 Agent（意图徽章/思考链/SQL 折叠）做成一等公民。流程见对话样机 → 老板批准方向后自主实现
  - **设计系统重建（含修真 bug）**：`tokens.css` 重写为**完整超集**——新语义令牌(`--ink/--accent/--line/--bg-*/--r-*/--sh-*`) + 全部历史命名 alias 指过去。**修复**：多组件引用了 tokens.css 里**根本没定义**的变量(`--bg-1-2/--data-dim/--up/--up-bg/--border-soft/--brand-border/--err/--maxw-thread` 等)→工作树原本半残；现一次补齐零破坏。`global.css` 补齐组件依赖却缺失的 keyframes(`spin/skeletonPulse` 等)
  - **重写组件**：`App`(命令中心↔对话双态、移动端抽屉) `Sidebar`(会话按时间分组/搜索/收藏看板入口/用户菜单) `TopBar`(薄/状态) 新 `CommandCenter`(门面 Hero+居中命令输入+按双脑能力分组示例+引擎概览) `ThinkingTrace`(横向步进器+意图徽章) `UserMessage/AssistantMessage`(统一操作栏) `DataResultCard`(+**查看 SQL 折叠**展示 Text2SQL) `KnowledgeAnswer` `Composer` `QuickSuggest` `LoginView` `KnowledgeBaseModal` `LogoMark`(准星镜头)；`utils/chart.js` ECharts 改**亮色主题**(发丝网格/等宽数字轴/车速红系列)。删 orphan `MarketKPI/MarketOverview/EmptyState`
  - **创业级新功能（前端 + 最小后端）**：
    - **收藏看板**：任意结果卡「收藏」存快照 → `BoardModal` 网格展示/重提/删除。`useBoard`+`api/board.js`(mock=localStorage/live=后端)；后端 `SavedInsight` 表 + `GET/POST/DELETE /api/insights`(JWT+user_id 隔离)
    - **一键分享**：生成只读公开链接 `/s/{token}`，`PublicShare.vue` 带品牌渲染(复用结果卡)=营销落地位。`api/share.js`；后端 `SharedInsight` 表 + `POST /api/share` + **公开免鉴权** `GET /api/public/share/{token}`；`main.js` 按 `location.pathname` 路由(不引 vue-router)；Caddyfile 已有 SPA fallback→生产 `/s/` 可达
    - **导出报告**：`utils/report.js` 一键导出 Markdown(结论+表+引用+SQL+品牌页脚)；**Toast** 反馈体系(`useToast`+`Toasts.vue`)
  - **后端**：`app/models.py` 加 SavedInsight/SharedInsight；`app/main.py` 加 5 端点；`deploy/postgres/initdb/sql/30-app-schema.sql` 补 `saved_insight`/`shared_insight` 表（dev SQLite 走 create_all 自动建）
  - **验收**：`npm run build` 618 模块通过；后端 TestClient E2E 全过（注册→登录→收藏 增/查/删、分享→公开读取、坏 token 404、未鉴权 401 隔离）；Playwright(mock)浏览器冒烟——登录/命令中心/数据卡(红渐变柱+SQL折叠+操作栏)/知识卡(意图徽章+引用溯源)/收藏看板**零控制台报错**
  - index.html：`html` 底色暗→亮 `#fafafa`(防暗闪)，favicon 改红镜头，Geist 补 800 字重
  - ⚠️ 注意：默认仍 **mock 模式**(`VITE_DATA_SOURCE`)，新功能 mock 用 localStorage、live 用后端，与现有 history.js 同套路
- ✅ 真知识库（免 pgvector 自包含 RAG）+ 离线真检索（2026-06-18，**前端原本只是写死两条假 PDF**）
  - **背景**：前端 mock 的知识库是 `api/kb.js` 里硬编码的两条假文档；用户要"真正的"。本机实况：BGE 权重在但 **model.safetensors 损坏**（SafetensorError），且无跑着的 PG/Docker。
  - **方案（用户选"两者都要"）**：① 生成真实新能源行业语料 `data/seed_kb/*.md`（8 篇：年度综述/政策补贴/技术路线/价格格局/品牌盘点/出口/智驾/口碑，可复用 5280 条口碑）；② 后端自包含 RAG：新增 `app/rag/local_store.py`（SQLite+numpy，**接口与 pg.py 完全一致**）+ `local_ingest.py`（解析→`chunk.build_chunks`→向量化→入库），`config.RAG_BACKEND`(默认 `local`) 切换，`retrieve.py` 仅把 `pg` 换 `store` 一处、检索/重排/归并/生成逻辑零改；③ 前端离线：`data/build_local_kb.py` 把语料灌库并导出 `frontend/src/api/kb_corpus.json`，`mock.js` 改为对它做**真实词法检索**(CJK 二元组重叠)+真实引用(抽取式)，`kb.js` 文档列表也取自该 JSON（不再假数据）。
  - **关键修复（稳定性）**：BGE 权重损坏 → `embed.py` 全面**优雅降级**（模型加载失败返回 None → `count_tokens` 走启发式、`embed_query/passages` 返回 None/[]）；`retrieve.hybrid_recall` 与 `local_store.search` 对 None 向量短路 → **RAG 自动降级为纯词法（jieba 全文 + RRF），无需 1.3GB 模型即真实可用**。bge-reranker 缺失同样降级 RRF。
  - **验收**：`build_local_kb` 灌 8 文档/79 chunk；后端 `answer_question` 实测 DeepSeek 出**带真实引用**答案（渗透率→《年度综述·总体规模与渗透率》；增程→《技术路线对比·三条路线本质》）；前端 mock 冒烟：问"增程和插混区别"→真实答案+3 条真实引用卡（章节+页），知识库弹窗列 8 真实文档，零控制台报错；`npm run build` 通过。
  - 想要语义检索：把 `models/bge-large-zh-v1.5/model.safetensors` 重新下全（ModelScope）即自动升级为向量+词法混合；或 `RAG_BACKEND=pg` 走全栈 pgvector。
- ✅ 管理员后台 + 用户管理 + 账户自助（2026-06-19，前后端，E2E + 冒烟全过）
  - **后端**：`User` 加 `role`/`disabled` 列（`database._migrate()` 幂等补列，SQLite/PG 通用；schema SQL 同步）；`auth.py` 加 `require_admin`、登录拒禁用账号、自助 `PATCH /api/auth/me`(改昵称)/`POST /change-password`/`GET /stats`(我的用量)、`bootstrap_admin`(启动确保管理员，`ADMIN_USERNAME`/`ADMIN_PASSWORD` 可配，默认 admin/admin123 带告警)；新 `app/admin.py`：`/api/admin/overview|users|users/{id}(PATCH)|.../reset-password|users/{id}(DELETE)`，护栏=不能禁用/降级/删除自己或最后一个管理员。
  - **前端**：`useAuth` 加 `isAdmin`/`setUser`；`api/admin.js`+`api/account.js`(mock↔live)；`AdminModal`(概览卡+用户表+改角色/禁用/重置/删除，自己行隐藏危险操作+「你」标)；`SettingsModal`(用量+改昵称+改密码)；Sidebar 加「管理后台」(admin 可见)+用户菜单「账户设置」。mock：用户名 `admin` 即获 admin 角色，离线可演示。
  - **验收**：后端 TestClient 全过（admin 登录/非 admin 403/列表概览/禁用→登录403/最后管理员护栏/重置/删除护栏/自助改名改密/stats）；Playwright mock 冒烟（管理后台入口仅 admin 见、用户表 2 行、禁用置灰、设置面板渲染）零报错；`npm run build` 通过。
- ✅ 全栈本地端到端跑通（2026-05-22，**前端 live 连真后端**）
  - 后端 `uvicorn:8000` + 前端 `vite:5173`，`frontend/.env` 切 `VITE_DATA_SOURCE=live`；真浏览器(Playwright)验收「2025年纯电Top10」：思考过程/ECharts 柱状图(真实车系名+销量)/结论流式/SQL·数据表折叠/「已连后端」全过，**无控制台报错**
  - **修复图表 bug**：live 下 X 轴全 `undefined`、无柱子。根因＝后端 `rows` 发「字典行」而前端按列位 `r[i]` 取值需「数组行」。改 `app/main.py` 的 `rows` 事件对齐 PRD-2 §9.1：`{columns, rows(数组)}`，前端零改动即正确
  - 启动步骤/根因/修复详见 `docs/工作记录/后端/2026-05-22-全栈本地端到端跑通.md`(+.docx)
  - ⚠️ 仍待对齐：SSE 其余事件名仍历史形态（`meta`/裸字符串/`[DONE]`/chart 是选图建议非完整 option），前端归一化层兜着能跑；RAG 脑、`/api/kb/list`、`/api/history` 后端未实现（前端已留位）
- ✅ 登录模块 / JWT 鉴权 + 用户隔离（2026-05-22，PRD-2 §17.3）
  - 新增 `app/models.py`(users/conversation/message/kb_document，均带 user_id) + `app/database.py`(独立**读写**库 `app.db`，与只读分析库 `bi_demo.db` 分开) + `app/security.py`(bcrypt+JWT) + `app/auth.py`(register/login/me + `get_current_user`)
  - `app/main.py`：挂 auth 路由；`/api/ask`、`/api/ask_sync` 加 `Depends(get_current_user)`；新增 `/api/history`、`/api/kb/list`（均按 user_id 过滤）；问答落 conversation/message 归属当前用户
  - `requirements.txt` 加 `passlib[bcrypt]`+`bcrypt==4.0.1`(钉版，见下)+`python-jose[cryptography]`；`.env` 加 `JWT_SECRET`/`JWT_EXPIRE_DAYS`/`APP_DATABASE_URL`；`sql/schema.sql` 补应用层 DDL
  - 验收全过：无 token→401；注册/重名 409/错密码 401/登录拿 token/`/me`；A 带 token 提问建会话；**会话与知识库隔离成立**（A history=1、B=0；A 的 kb 文档 B 看不到）
  - 测试账号(本地 app.db，已 gitignore)：`alice/pwA123`、`bob/pwB123`
  - ✅ **前端已配合（2026-05-22）**：前端登录模块完成并对齐后端实测契约——登录/注册页(`LoginView.vue`,nint 风格深色品牌 hero+表单分屏)；token 存 localStorage,全局请求(含 SSE fetch)带 `Authorization: Bearer`;路由守卫(无 token 进不了主界面);顶栏昵称+退出;会话历史按用户分桶隔离。注意 register **不发 token**→前端注册后自动 login;错误体读 `detail`;`/api/kb/list` 读 `{documents}`;`meta`/`done` 的 `conversation_id` 前端回带续接同一会话。**起真后端 live 联调通过**：curl 验证 register/login/重名409/错密码401/未授权401;浏览器注册→自动登录→进主界面+存真实 JWT,无报错。详见前端工作记录 §9
  - ⚠️ **踩坑**：passlib 1.7.4 与 bcrypt>=4.1/5.x 不兼容（自检>72字节抛错）→ 钉 `bcrypt==4.0.1`
  - 详细：`docs/工作记录/后端/2026-05-22-登录模块.md`(+.docx)；前端侧 `docs/工作记录/前端/2026-05-22-前端门面.md` §9
- ✅ 图表描述符改造（2026-05-22，T8）：后端不再「LLM 生成写死的 option」，改**规则引擎产出描述符 + rows**，前端自由拼图/切图型/图例
  - `app/charts.py` 重写为规则引擎（删 LLM）：看结果集结构（数值列/时间列/文本列、类别数）判图型；`app/agent.py` 传 question 作标题
  - **chart 事件新契约**：`{default_type, applicable_types[bar/hbar/line/pie/table], dimension(列名), measures(列名数组→多系列+图例), title}`；`rows` 照发(前端画图数据源)；`sql` 仍发但前端不展示
  - 规则：无数值列→table；文本维度→bar(类别>12→hbar，单系列≤8类别加 pie)；仅时间列→line(year/month 算维度不算度量)
  - 收益：每问 LLM 调用减一次（选图零延迟/不抽风），LLM 只留结论/归因
  - ✅ **前端已适配（2026-05-22）**：`utils/chart.js` 的 `normalizeChartSpec()` 消费新描述符（`measures` 多列→多系列+图例；`default_type='table'`→不出图只看结论），兼容旧 `{chart_type,x,y}`/完整 option。数据卡只留「图表+结论」（去掉查看SQL/数据表）、右上按 `applicable_types` 出**图型切换器**(柱状/折线/饼/横向条形)、默认用 `default_type`，**切换在前端用同一份 rows 重绘不重新请求**，所有图型开图例。Playwright 验收：4 图型切换+默认+图例全过、无报错
  - 详细+契约：`docs/工作记录/后端/2026-05-22-图表描述符改造.md`(+.docx)；前端侧 `docs/工作记录/前端/2026-05-22-前端门面.md` §5.3
- ✅ T14 本地「生产同款」基础设施（2026-05-25，**docker-compose 一条命令起全栈，DoD 四条全过**）
  - 新增 `deploy/docker-compose.dev.yml`：PostgreSQL16+pgvector / MinIO(自动建桶) / Redis，全带 healthcheck；起栈 `docker compose --env-file ../.env -f docker-compose.dev.yml up -d`
  - **PG 双库 + 角色隔离（生产同款）**：`bi`(分析库 dim_*/fact_*，**只读**角色 `bi_readonly`) + `app`(应用库，**读写**角色 `app_rw`)；两库都 `CREATE EXTENSION vector`(pgvector 0.8.2)。实测只读角色写 bi 被拒、读写角色写 app 成功
  - 初始化：`deploy/postgres/initdb/00-init.sh`(首次启动跑：建库/建角色/装扩展/建表/授权) + `sql/10-bi-analysis.sql`(分析表，源自 `sql/schema.sql`) + `sql/30-app-schema.sql`
  - **关键决策**：app 库的 users/conversation/message/kb_document **对齐当前后端 `app/models.py`**(主键 `id`、外键 `conversation_id`)，**不是** `sql/schema_app.sql` 的 `user_id/conv_id/msg_id/doc_id` 命名——否则后端 ORM 查 `users.id` 会报列不存在、联调全挂。`kb_chunk`(父子分块 + `vector(1024)` + HNSW partial 索引 `idx_kbchunk_vec`)取自 schema_app.sql，外键改指 `kb_document(id)`；`kb_document` 另补 file_type/source_uri/title/chunk_count/deleted_at(nullable)留给后续 RAG/MinerU 入库（后端 ORM 暂不读写）
  - MinIO 两桶：`kb-uploads`(用户上传) / `crawl-raw`(爬取原始)；access key/secret = `MINIO_ROOT_USER`/`MINIO_ROOT_PASSWORD`；控制台 http://localhost:9001
  - `.env.example` 补全 PG/MinIO/Redis 连接串 + infra 段(库名/角色/密码/端口) + **SQLite↔PostgreSQL 切换说明**：demo 默认 SQLite，联调把 `DATABASE_URL`/`APP_DATABASE_URL` 改成 PG 串即可（代码零改动，config.py 只读这两个变量）。`requirements.txt` 加 `psycopg[binary]`；`.gitattributes` 钉 `*.sh eol=lf`
  - **连接串（已交付后端，本地 dev 占位密码见 .env.example）**：
    - 分析库(只读) `DATABASE_URL=postgresql+psycopg://bi_readonly:<pw>@localhost:5432/bi`
    - 应用库(读写) `APP_DATABASE_URL=postgresql+psycopg://app_rw:<pw>@localhost:5432/app`
    - MinIO `endpoint=localhost:9000`(控制台 9001)；Redis `redis://:<pw>@localhost:6379/0`
  - 实测验收(2026-05-25)：4 服务全 healthy(minio-init Exited0)；pgvector 0.8.2 双库；bi 6 表 / app 5 表齐；只读/读写隔离生效；HNSW 索引在；Redis PONG；MinIO 两桶 mc ls 列出
  - **踩坑**：①`wsl --install` 走商店被 **403**(CN 网络)→ 从 github.com 直下 `wsl.2.7.3.0.x64.msi` 装(github 网页通、api.github.com 限流)，或专业版改 Hyper-V 后端绕过；②`.env` 值行**别留行内注释**——compose 读 `--env-file` 会把 `#注释` 连空格当成值(密码/桶名出错)，已把 .env.example 注释全改单独成行；③容器脚本须 LF 行尾否则 `bad interpreter`
  - 详细：`docs/工作记录/运维/2026-05-25-本地生产同款基础设施-docker-compose.md`(架构图 + 步骤 + 踩坑 + 实测)
- ✅ T2 收尾 / 补采口碑+车系详情 + 回填（2026-05-25）
  - **数据源**：销量接口给不了 score/segment/续航，从懂车帝**口碑页** `GET /auto/series/score/{series_id}` 补——
    SSR 站点候选 JSON 接口全 404，但页面 `<script id="__NEXT_DATA__">` 含整页数据，扒 `props.pageProps` 一次拿全（免登录）
  - **采集** `data/crawl_koubei.py`（沿用 crawl_sales 范式：scrapling venv/page.body/随机延时/指数退避/manifest 幂等，按车系遍历 dim_series 409 id）→ 409 全采、0 空、detail=409、reviews=5280
  - **回填** `data/clean_load.py`（全量重建时并入口碑详情）：score=total_score÷100→`fact_review.score`；segment=car_type、endurance_km=续航上限→`dim_series`；powertrain 仍按能源类型推断不覆盖。**按 series_id 对齐，409=409 无重复车系**
  - 覆盖：segment 406/409、endurance 403/409、score 256/409 有真实分（其余评分=0→诚实 NULL）；Model Y 抽查=中型SUV/纯电/821km/4.0 ✅
  - **RAG 语料** `data/export_rag_corpus.py` → `data/rag_corpus/dongchedi_koubei_reviews.jsonl`：5280 篇/393 车系/344 万字（路②种子语料第一块，见下方设计决策）
  - 新解锁分析：「续航>600km 纯电车系」「中型SUV 销量 Top」「口碑分最高」等
  - `sql/schema.sql` dim_series 加 `endurance_km`；字段清单 `data/probe/口碑车系详情字段清单.md`
  - 详细：`docs/工作记录/后端/2026-05-25-补采口碑车系详情回填.md`(+.docx，2 张 mermaid 图)
- ✅ T9 RAG 离线入库管线（2026-05-25，**全实现 + 本地 E2E 全 DoD 通过**）
  - 链路：上传→存 MinIO→`kb_document(parsing)`→**Celery 异步**解析→**结构感知父子分块**→**BGE-large-zh** 给子块算 1024 维向量→写 **PG+pgvector**（HNSW partial 索引）→`status=ready/failed`
  - 新增 `app/rag/`：`store.py`(MinIO)/`parse.py`(**可插拔解析器** PyMuPDF+pdfplumber，MinerU 留接口 `PARSER_BACKEND`)/`chunk.py`(父子分块+overlap+表格整体+标题增强+token控制)/`embed.py`(BGE passage不加前缀+归一化+token计数)/`pg.py`(psycopg+pgvector 持久层，重传软删+父子两段写+预过滤检索)/`ingest.py`(编排)/`tasks.py`；`app/celery_app.py`；`app/kb.py`(`/api/kb/upload`+状态轮询)
  - **父子分块**：子块~280token(`is_retrievable=true`,有 embedding,进检索)；父块=完整小节~900token(`is_retrievable=false`,无 embedding,命中后回填)；同表 `level` 区分、子块 `parent_chunk_id` 指父块。512token 是 BGE 硬上限，按 token 不按字符；标题增强=`heading_path` 拼进 `content_embed`（展示用 `content` 不含，分开防污染）
  - `KbDocument` ORM 补 file_type/source_uri/title/chunk_count/deleted_at；`kb_chunk`(含 vector(1024)) **不进 ORM**，走 PG 原生 SQL（SQLite 建不了向量列）；模型名/版本入 config(`EMBED_MODEL_VERSION`)，换模型必须全量重建(§5.8)
  - 依赖：celery/redis/minio/pgvector/pymupdf/pdfplumber/sentence-transformers(torch CPU)/python-multipart/reportlab(测试造PDF)；BGE 权重从 **ModelScope** 下到 `models/`(gitignore)，`EMBED_MODEL_NAME` 指本地目录
  - **E2E 全过**(`data/rag_ingest_demo.py`)：带表格 PDF→父块2/子块8/表格整块1/token≤234；向量检回(score0.554)→换父块→引用「章节+页码1」；重传 10→10 无重复(旧版软删)；评论语料入库(悦也Plus 64chunks)可检回
  - ⚠️ **踩坑**：①本机原生 PG17 占 5432→容器 PG 用 `POSTGRES_PORT` 避让 **5433**(数据 volume 不丢)，`.env` 加 `RAG_DATABASE_URL`(localhost:5433)；②容器内 127.0.0.1 是 trust 认证(任意密码能进，迷惑)；③huggingface_hub 连 hf-mirror 失败→改 ModelScope；④FastAPI 上传需 `python-multipart`；⑤Celery on Windows 用 `--pool=solo`
  - 详细：`docs/工作记录/后端/2026-05-25-RAG离线入库管线.md`(+.docx，含「文档→Markdown→父子分块→向量」流程图)
- ✅ T9 RAG 在线问答（2026-05-25，**检索→归并→带引用生成全实现 + 企业级真实库 E2E 全 DoD 通过**）
  - `app/rag/retrieve.py`：query 加指令前缀向量化 → **混合召回**(向量 top20 + PG 全文 top20，**RRF 融合** k=60) → **bge-reranker 重排**取 Top-K(=8，下不到模型降级 RRF 分) → **父块归并四情形** → 拼来源头+强约束 Prompt → DeepSeek 出 `{answer, citations:[{doc_id,page_no,chunk_id}], has_answer}`
  - **父块归并(§5.4.1)**：A 多子块同父→去重取 max 分；B 相邻父块(`doc_parent_order` 判位次)→合并连续窗口；C 互补→按分排序+token预算≤3000+父块≤5(防 lost-in-the-middle)；D 冲突→不合并不取平均，Prompt 强制并列标注来源/口径
  - **防幻觉双闸**：召回空 或 最高重排分<0.30 → has_answer=false「未找到依据」；**防注入**：Prompt 约束片段内指令只总结不执行
  - **隔离**：召回一律 `WHERE user_id AND deleted_at IS NULL` 预过滤(防 HNSW 后过滤召回不足 + 多租户)
  - `pg.py` 加 `keyword_search`/`get_parents_full`/`doc_parent_order`；`embed.py` 加 `rerank_scores`；`app/kb.py` 加 `POST /api/kb/ask`
  - **企业级知识库**(按用户要求用真实多源多格式，非玩具)：`data/crawl_seed_corpus.py` **scrapling 爬乘联会 cpcaauto.com 真实行业新闻/政策 40 篇**(md，路②种子) + 多页研报 PDF(reportlab 生成《2025市场年度报告》7章/多表) + 全量懂车帝口碑(393车系/5280评论，按车系成文档)；`data/rag_build_kb.py` 统一灌库
  - **E2E 全过**(`data/rag_retrieve_demo.py`，对 51 文档/757 chunk 真实库)：问答带引用可溯源(reranker 相关分 0.988/0.999)；多父归并 context_parents=2~5；冲突 45%/58% **并列不取平均**各带引用；无关问题 top_score=0.033→不幻觉；用户隔离 B 查不到 A、无泄漏
  - **踩坑**：CrossEncoder 已自带 sigmoid 别二次套；reranker id 用 `BAAI/bge-reranker-base`(可降级)；pgvector 检索参数 `%s::vector` 显式转；`.env` LLM 段曾被重置占位→恢复 DeepSeek v4-pro
  - 详细：`docs/工作记录/后端/2026-05-25-RAG在线检索-归并-引用.md`(+.docx，全链路+四情形流程图)
- ✅ 数据采集与语料构建·总说明（2026-05-25）：`docs/工作记录/后端/数据采集与语料构建-总说明.md`(+.docx) 把所有爬虫/清洗/语料脚本(probe/crawl_sales/crawl_koubei/crawl_seed_corpus/clean_load/export_rag_corpus/rag_build_kb/ingest 管线)的逻辑、文件关系、两库怎么建、企业级程度讲清，附全局流程图。**澄清：RAG「语料库」是 PG kb_chunk 表里的「切块文本+向量」，md/PDF 只是中转原料**
- ✅ 企业级 RAG 知识库（2026-05-25，真实多源多格式，非玩具）：`data/crawl_seed_corpus.py` scrapling 爬**乘联会 cpcaauto 行业新闻/政策 36 篇**(只爬有文字正文的 news 类；车市解读 csjd 类是图片无正文→拒收，质量校验拒导航垃圾) + 多页研报 PDF + 全量懂车帝口碑(393车系) → `data/rag_build_kb.py` 灌库
- ✅ T10 LangGraph 双脑编排（2026-05-25，**11 节点状态图替换简化分支，E2E 全 DoD 通过**）
  - `app/graph.py`：`AgentState`(TypedDict，trace 用 `Annotated[list,operator.add]`) + 节点 intent_router/clarify/schema_link/gen_sql/exec_sql/fix_sql/chart/insight/rag_retrieve/rag_answer/compose；复用 text2sql/charts/insights/rag.retrieve 现成函数
  - **条件路由(§7.5)**：intent_router→sql/rag/hybrid/clarify；exec_sql→成功 chart／失败可重试 fix_sql(回 exec_sql 成**环**)／耗尽 insight(降级不塞错 SQL)
  - **hybrid 并行**：条件边返回 `["schema_link","rag_retrieve"]` 两链并行；**compose `defer=True` 做 join**(否则两链长度不一 compose 跑两次——踩坑)；compose 合「数据结论+文档佐证」
  - **意图路由(§7.4)**：规则前置(含销量/排名→sql，报告/政策/口碑→rag，跳过 LLM 省成本) + LLM few-shot 兜底(带歧义→clarify 示例)
  - **可观测**：每节点写 `trace`(节点+决策+SQL+重试)，供评测/调试
  - E2E(`data/graph_demo.py`)全过：四意图正确路由；hybrid 两链并行 trace 交错 compose 单次 join；SQL 失败重试/耗尽降级；trace 含生成 SQL 可回溯
  - `requirements.txt` 加 `langgraph>=0.2`；详细 `docs/工作记录/后端/2026-05-25-LangGraph双脑编排.md`(+.docx，状态图)
- ✅ 后端对齐 §9.1 SSE 协议 + 补全 §10 接口（2026-05-25，E2E 全 DoD 通过）
  - `/api/ask` 改用 **LangGraph `run_agent`**，按 §9.1 发命名事件：`intent`/`sql`(仅审计,JSON{sql_text})/`rows`({columns,数组行})/`chart`(描述符)/`insight`({delta}逐段)/`citation`/`done`/`error`；纯 RAG 无 sql/rows/chart 只有 insight+citation
  - **✅ 前端 SSE 归一化兼容层已撤（2026-05-25）**：旧差异(meta→intent、cols→columns、裸串→JSON、done=[DONE]→{msg_id}、chart 旧格式、无 citation 事件)已全部对齐，前端 `events.js` 直接按事件名/JSON 消费（见上「T12 前端续作」）
  - 补全 §10 接口(全 JWT + user_id 隔离)：`/api/kb/upload`(类型/大小校验,415/413/400 结构化错误) `/api/kb/list`(过滤软删) `GET /api/kb/{id}`(状态轮询) `DELETE /api/kb/{id}`(软删 set deleted_at) `/api/history`+`/api/history/{conv_id}`(还原含图表/引用)
  - `Message` 加 `result_meta`(JSON 存 chart/列行/citations/trace)→历史会话**重绘图表+还原引用卡**无需重跑 Agent；移除旧 SQLite 版 `/api/kb/list`
  - E2E(`data/api_demo.py`，app 跑 PG)全过：未鉴权401；上传→ready→问答(含1234+引用)；SSE 数据脑`intent→sql→rows→chart→insight→done`+chart描述符、纯RAG`intent→insight→citation→done`；history 还原 chart/citations；软删后问不到
  - **踩坑**：①Redis 容器无密码(requirepass 未生效)→ REDIS_URL 去密码；②Celery `.delay()` 默认等 result backend 阻塞→ `task_ignore_result=True` fire-and-forget；③`Message` 加列要手动 ALTER PG(create_all 不改已存在表)
  - 详细：`docs/工作记录/后端/2026-05-25-SSE协议对齐与接口清单.md`(+.docx，SSE 事件对照表+接口清单+事件流图)；**联调时通知前端撤兼容层**
- ✅ 全量企业级 RAG 知识库建成（2026-05-25）：rag_demo **432 文档 / 28607 chunk / 22356 可检索子块**（口碑 395 + 乘联会 36 + 研报 PDF 1）
- ✅ T13 RAG 评测台 + 基线（2026-05-25，§12.2）
  - **评测集免标注** `data/rag_eval_build.py`：反向生成(从已知父块让 LLM 出「只能用这段回答的题+答案」→ 该父块即 ground-truth)，分层+难度梯度(single38/cross6/negative10)=54 题 → `data/rag_eval/eval_set.jsonl`
  - **评测** `data/rag_eval_run.py`：检索(recall/precision/MRR)+生成(faithfulness/correctness LLM-judge)+防幻觉(abstain) 分开
  - **基线**：recall=0.455(口碑0.75/乘联会0.28/研报0.30)、MRR=0.388、faith=0.34、correct=0.42、**abstain=1.000(零幻觉)**；precision=0.081 是单-gt 指标特性
  - **🔑 评测定位真缺陷(已验证)**：全文路 PG `simple` **对中文不分词→中文 query 召回 0**('800V' 等纯英数才有效)→混合召回退化纯向量→政策/数字类(乘联会/研报)召回垮
  - ✅ **已修复+回归(闭环)**：jieba 应用层分词(新列 `content_tokens`+查询切词 OR tsquery，`app/rag/text.py`+`pg.py`，存量 28629 chunk 用 `data/rag_fts_backfill.py` 回填免重算向量)。回归同集：**recall 0.455→0.830、乘联会 0.28→0.89、研报 0.30→0.85、跨段 0.17→0.92、correct 0.42→0.80、faith 0.34→0.71、abstain 1.0 保持**。schema(30-app-schema.sql) + requirements(jieba) 已同步
  - 详细：`docs/工作记录/后端/2026-05-25-RAG评测台与基线.md`
- ✅ T13 测试与评测体系（2026-05-25，测试）：在后端 RAG 评测台基础上，建**三套可量化评测 + pytest + CI + 数据质量**
  - **意图路由**(100条)：整体准确率 **89%**（clarify P/R=1.0、sql R=1.0；**rag recall 0.74**——11 条错分全是「问文档里的数字/趋势」被规则前置按关键词误判为 sql）
  - **Text2SQL**(60条)：按**执行结果集等价**比对，EX **78.3%**（带自校验重试，首次76.7%，**重试增益+1.7pp**——重试只救执行报错、救不了「能跑但语义错」）
  - **RAG**(60条,RAGAS口径,DeepSeek judge)：**防幻觉拒答率100%**、**多源冲突并列100%**、context precision 0.92/recall 0.89/faithfulness 0.80/answer relevancy 0.94；应答率0.75（大库28607chunk下约1/4跨段题被阈值拒答）
    - **检索消融**：纯向量 hit-recall 0.750 = 混合召回 0.750 < +rerank 0.762 ——**印证后端发现**：全文路 PG simple 中文不分词→混合召回退化为纯向量(vec==hybrid)，rerank 仅 +1.2pp
  - **数据质量**(18/18 expectations)：行数/非空/值域/唯一/外键/口径；**发现** DOMAIN 称 fact_review.score 恒NULL，但补采回填后已有 5858 条值[0,5]→建议更新 Text2SQL DOMAIN
  - **pytest**(tests/ 覆盖采集/清洗/Text2SQL/RAG/Agent/接口关键路径) + **GitHub Actions**：PR 自动跑单测 + 评测红线（读 eval/reports/*.json，低于阈值阻断合并）
  - 取舍：RAGAS/DeepEval/GE 按原定义**自实现**（真装 ragas 会破坏 openai/jiter 依赖链）；冲突类用隔离用户测；执行结果比对而非 SQL 文本比对
  - 文件：`eval/`(common/intent_eval/text2sql_eval/rag_eval/data_quality/datasets/reports)、`tests/`、`.github/workflows/ci.yml`、`pytest.ini`、`requirements-dev.txt`；详细 `docs/工作记录/测试/2026-05-25-评测体系.md`
  - ⚠️ 给后端的发现：① 意图规则过宽（建议加文档实体词优先级/LLM 兜底，提 rag recall）；② 大库下 RAG 长尾召回受限（呼应「中文分词」修复，并建议增大 RECALL_K/按文档类型加权）；③ DOMAIN 的 score 口径过时
- ✅ T14 生产化部署（2026-05-26，运维，**部署物全套就绪，compose 校验+Dockerfile lint 通过**）
  - **Dockerfile**：`deploy/Dockerfile.api`(api/worker/beat 共用，含 torch/BGE+scrapling，模型走挂载卷不进镜像) + `deploy/Dockerfile.web`(Vite 构建→Caddy 静态)
  - **`deploy/docker-compose.prod.yml`**：一键起 Caddy+FastAPI+Celery(worker+beat)+PG(pgvector)+Redis+MinIO；模型卷/数据卷/证书卷持久化；**仅 Caddy 暴露 80/443，PG/Redis/MinIO 仅内网**
  - **Caddy 自动 HTTPS**(`deploy/Caddyfile`)：前端静态+`/api`反代(SSE flush_interval -1)+Let's Encrypt+安全头；CORS 经 `CORS_ALLOW_ORIGINS` 收紧到正式域名(app/config+main 已改可配)
  - **数据迁移** `deploy/load_analysis_pg.py`：raw→PG bi(初始 8072 行+月度刷新，复用 clean_load 清洗)；`10-bi-analysis.sql` 补 endurance_km
  - **定时采集**：`app/tasks_cron.py`+beat 月度 `cron.monthly_sales_refresh`(采集→加载 PG)；**备份** `deploy/backup/backup.sh`+`restore.sh`(PG+MinIO,14天轮转,host cron)
  - **`.env.prod.example`**(全占位强调换强密钥) + **`deploy/DEPLOY.md`**(空服务器→HTTPS 全流程:选型[香港免备案/大陆ICP]/装Docker/密钥/域名/下模型/起栈/导数据/建账号/验收/备份/回滚) + **`Makefile`**(models/build/up/load-analysis/register/backup/restore/health) + `deploy/download_models.sh`
  - **CI**：在测试团队 ci.yml(单测+评测红线)基础上**追加** build-images job(校验 compose+构建 api/web 镜像，push main 触发)
  - **安全基线**(§17/§18)：dev 默认值(JWT dev-insecure/口令 change_me/CORS *)上线前全换；密钥只在 .env.prod(gitignore)
  - 详细：`docs/工作记录/运维/2026-05-26-生产部署-prod-compose-HTTPS.md`(+.docx，架构图+回滚+踩坑)
- ⏭️ 下一步候选：
  1. 真买服务器照 DEPLOY.md 跑一遍上线（域名 HTTPS）；可选 pg_jieba 扩展替代应用层分词
  2. MinerU 接入(`PARSER_BACKEND=mineru`，需 ≥8G 内存或独立解析节点)
  3. 评测扩到 80 条；推 GHCR + 自动部署(watchtower/SSH)

## 产品决策（图表交互，2026-05-22）
- 数据分析结果**不展示 SQL、不展示数据表**，只给「图表 + 一句话结论/归因」。sql_text 仍存库审计，不推前端展示。
- 图表**用户可切换图型**（柱状/折线/饼/横向条形…）且**带图例(legend)**，切换是**纯前端本地重绘**（数据已在手，无需重新请求后端）。
- 后端 chart 输出从「写死的 ECharts option」改为「**图表描述符**」：`{default_type, applicable_types[], dimension, measures[], title}` + 随附 rows；前端据此自建 option。图型判定用**规则引擎**（不用 LLM），LLM 只写洞察。
- 已落进 PRD-2 §8.2/8.3、§9/§9.1（chart 事件=描述符）、§19.8/§19.10。

## 设计决策（RAG 语料来源＝两路，2026-05-25，用户拍板）
RAG 知识库语料**两条路并存，缺一不可**，都走同一条管线（MinerU→结构感知父子分块→BGE 向量化→`kb_chunk`/pgvector→混合召回→rerank→父块回填→引用），差别只在「这块语料属于谁」：
- **路① 用户上传（产品核心/护城河）**：登录用户传自己的研报/政策/企业资料，按 `user_id` 隔离（登录模块+kb_document/kb_chunk+MinIO `kb-uploads` 桶已就位；缺 `/api/kb/upload`+MinerU 解析=T5/T9）。**A 传的只有 A 能问到**，是付费理由。
- **路② Scrapling 爬种子语料（解决冷启动/开箱即懂行）**：挂公共账号、所有人可检索。为什么要：只有路①则新用户库空、RAG 答不出，第一印象就废。爬什么：**乘联会**(权威销量解读/渗透率口径)、**政策**(购置税/双积分/补贴，纯文本只能 RAG)、**垂媒**(评测/分析稿)、**懂车帝口碑评论**(UGC 口碑/情绪，"开起来怎么样"数仓答不了)。
- **当前进度**：路②的**口碑评论已落地**(`data/rag_corpus/`，5280 篇)，是种子语料第一块；乘联会/政策/垂媒种子爬虫是后续扩展(可另起工单)。
- 一句话：**路①是产品（私有/个性化），路②是底座（公共/冷启动）。**

## 设计决策（RAG 切块与检索，2026-05-22）
> 把 RAG 这块讲透到「面试深挖也不怕」的程度，已重写 PRD-2 §5 整章 + §6.2 + §19.4/19.6，并改 `sql/schema_app.sql` 的 kb_chunk。
- **纠正「切 500-800 字」**：真正硬约束是 BGE-large-zh **512 token 上限**（超了截断丢语义），按 token 算不按字符；子块目标 ~250-300 token。
- **切块=结构感知 + 父子分块（small-to-big）**：先按 MinerU 的 Markdown 结构（章/节/段）递归切、句子边界、overlap ~50-80 token、表格整体保留、**标题增强**（embed 文本前拼 heading_path）。子块(~250-300t, is_retrievable=true, 有 embedding)做检索；父块(完整小节~800-1000t, is_retrievable=false, 无 embedding)做上下文回填。父子同表用 `level` 区分，子行 `parent_chunk_id` 指父行。
- **在线检索管线**：query 加 BGE 指令前缀（query/passage 非对称）→ **混合召回（向量 + BM25/全文，RRF 融合）**补型号/数字/专名弱项 → bge-reranker → **父块归并** → 生成带引用。
- **「子块命中多个父块」四情形处理（用户重点问题）**：A 同父去重(取 max 分)；B 相邻父合并连续窗口去重叠；C 不同父信息互补→按分排序+token预算(≤3000)+父块数上限(≤5,防 lost-in-the-middle)+每块带来源头供分别归因；D 父块信息冲突→不取平均、显式并列标注来源与时间口径、各自 citation。确定性规则兜住，不把碎块直接塞 LLM。
- **kb_chunk 新增字段**（从「检索→回填→溯源→隔离→评测」链路反推，PRD-2 §5.6 有逐字段推导表）：`user_id`(冗余,免 join 过滤)、`level`、`parent_chunk_id`、`is_retrievable`、`chunk_type`(text/table/title)、`heading_path`、`content_embed`(送 embed 文本，与展示 content 分开)。索引：HNSW partial(WHERE is_retrievable)、parent、user、可选全文 gin。
- **防幻觉**：无召回/低于阈值→澄清或「未找到依据」；检索内容含注入文本只总结不执行。
- **(2026-05-22 续)修两处 + 补运维盲点**：
  - 修硬伤：PRD-2 §19.1 删掉「车系树(汽车之家交叉校验)」，对齐**懂车帝单源**(避免实体对齐)+增量采集+补口碑/详情接口。
  - 补 §12.2「RAG 评测集怎么构造」：讲清 RAGAS 四指标各测哪段(context precision/recall 测检索、faithfulness/answer relevancy 测生成)；50-80 条问答对构造法(选语料→出问题+标准答案+ground-truth依据→难度梯度含跨段/冲突/库里没有四类→跑分定位)；Text2SQL 按执行结果比对。
  - 补 §5.8「工程鲁棒性」：embedding 版本一致性(换模型必重建索引)、文档更新/重传(软删旧 chunk 重建)、表格 table-to-text(摘要后再 embed、回填原表)、多租户**预过滤**召回(防 HNSW 后过滤召回不足)、异步失败可恢复。
- PRD-2 现 689998 字节，关键词校验全过。

## 踩过的坑（省得重踩）
- scrapling 必须用专用 venv；取内容用 `page.body` 不是 `str(page)`。
- 系统 Python 3.14 alpha 装不了很多包(lxml/scrapling 等)；docx 校验脚本因 lxml DLL 跑不了，改用内置 zipfile+minidom 自校验。
- 本机无 LibreOffice，docx 无法转 PDF 预览，靠结构校验 + 让用户在 Word 里看。
- 前端：原生 `EventSource` 只支持 GET，而 `/api/ask` 是 POST → 用 `fetch`+`ReadableStream` 自己解析 SSE。
- 前端：当前后端 SSE 协议与 PRD §9.1 有出入（见 T12 条目），前端做了归一化层兼容两套；后端宜逐步对齐 §9.1。
- Playwright 在系统 Python 3.14 下 sync API 会段错误（greenlet 不兼容）→ 冒烟脚本改用 `async_playwright` 异步 API。
